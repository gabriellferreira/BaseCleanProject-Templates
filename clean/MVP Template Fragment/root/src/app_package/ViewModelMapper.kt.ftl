package ${packageName}.presentation.view.mapper

import javax.inject.Inject
import ${packageName}.presentation.view.model.${viewModel}
 
class ${viewModel}Mapper @Inject constructor() {
	
	fun map(): ${viewModel} =
		${viewModel}()
}